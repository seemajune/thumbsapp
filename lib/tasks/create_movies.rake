namespace :create_movies do
  # bf = BadFruit.new('hk73sdh9btjw5w9t6m2cws9p')
  # in_theaters = bf.lists.in_theaters
  # in_theaters.each do |m|
  #     movie = Movie.new(:title => m.name)
  #     #binding.pry
  #      m.reviews.each do |r|
  #       # binding.pry
  #        movie.movie_reviews.build(:quote => r.quote)
  #      end
  #   movie.save
  # end
end
